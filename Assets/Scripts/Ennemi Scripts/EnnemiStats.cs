using UnityEngine;

public class EnnemiStats : MonoBehaviour

{
    [Header("Stats Joueur")]
    public float VieEnnemi = 60;
    // Start is called once before the first execution of Update after the MonoBehaviour is created

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
